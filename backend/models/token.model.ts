export default class Token {
    constructor(
        public id: number,
        public name: string,
        public symbol: string,
        public price: number,
        public logo: string
    ) {}
}
